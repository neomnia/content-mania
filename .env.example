# =============================================================================
# Configuration Neon PostgreSQL
# =============================================================================
# Obtenez ces valeurs depuis votre dashboard Neon : https://console.neon.tech

# URL de connexion principale (avec pooler PgBouncer - recommandé pour la plupart des usages)
DATABASE_URL=postgresql://[user]:[password]@[endpoint]-pooler.[region].aws.neon.tech/[database]?sslmode=require

# URL de connexion sans pooler (pour les opérations nécessitant une connexion directe comme les migrations)
DATABASE_URL_UNPOOLED=postgresql://[user]:[password]@[endpoint].[region].aws.neon.tech/[database]?sslmode=require

# Paramètres individuels PostgreSQL (optionnel - pour construction manuelle de l'URL)
PGHOST=[endpoint]-pooler.[region].aws.neon.tech
PGHOST_UNPOOLED=[endpoint].[region].aws.neon.tech
PGUSER=[user]
PGDATABASE=[database]
PGPASSWORD=[password]

# =============================================================================
# Configuration Vercel Postgres (compatibilité - optionnel)
# =============================================================================
# Ces variables sont utilisées par certains templates Vercel
POSTGRES_URL=postgresql://[user]:[password]@[endpoint]-pooler.[region].aws.neon.tech/[database]?sslmode=require
POSTGRES_URL_NON_POOLING=postgresql://[user]:[password]@[endpoint].[region].aws.neon.tech/[database]?sslmode=require
POSTGRES_USER=[user]
POSTGRES_HOST=[endpoint]-pooler.[region].aws.neon.tech
POSTGRES_PASSWORD=[password]
POSTGRES_DATABASE=[database]
POSTGRES_URL_NO_SSL=postgresql://[user]:[password]@[endpoint]-pooler.[region].aws.neon.tech/[database]
POSTGRES_PRISMA_URL=postgresql://[user]:[password]@[endpoint]-pooler.[region].aws.neon.tech/[database]?connect_timeout=15&sslmode=require

# =============================================================================
# Configuration NextAuth.js
# =============================================================================
# Générer avec : openssl rand -base64 32
NEXTAUTH_SECRET=your-secret-key-minimum-32-characters

# URL de l'application (adapter selon l'environnement)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# =============================================================================
# Variables optionnelles
# =============================================================================

# Clé secrète pour l'endpoint /api/setup (initialisation DB)
# SETUP_SECRET_KEY=your-setup-secret-key

# Clé admin pour les opérations sensibles
# ADMIN_SECRET_KEY=your-admin-secret-key

# Forcer la réinitialisation de la DB (utiliser avec précaution !)
# FORCE_DB_RESET=true
